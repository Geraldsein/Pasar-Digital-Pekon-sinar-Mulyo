# AGENTS.md

## Workflow
- Setiap permintaan fitur/perubahan baru: jalankan dan uji di localhost dulu (`npm run dev`).
- Jangan `git commit` atau `git push` kecuali diminta eksplisit.

## Commands
- Dev: `npm run dev`
- Lint: `npm run lint`
- Build: `npm run build`
